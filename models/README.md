# Model checkpoints

The notebook writes the best validation checkpoint to `models/model.pth`.

The previous project folder contained checkpoints with incompatible vocabulary sizes or parameter names, so no checkpoint is included here. Run the training cell to generate a model that matches the cleaned notebook.

Files ending in `.pth` or `.pt` are ignored by Git.

